<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.jsq.forum.dao.UserDao">
    <insert id="addUser" parameterType="com.jsq.forum.model.User">
        insert into user (username,password,introduction,created_date) values (#{username},#{password},#{introduction},#{created_date})
    </insert>
    <select id="getUserByUsername" resultType="com.jsq.forum.model.User">
        select *
        from user b
        where b.username= #{username}
    </select>
    <select id="getUsernameById" resultType="String">
        select username
        from user
        where id = #{userId}
    </select>
</mapper>